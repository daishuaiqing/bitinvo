- var typeLabel = ""
.col-lg-4.col-md-6.col-sm-12.grid-list-cell-holder.animated.fadeInUp(class='sm-delay-' + index)
  .grid-list-cell(data-id=id,data-canid=canId,data-cabinet=code, data-name=name,data-ismaster=isMaster?"true":"false",data-islocal=isLocal?"true":"false")
    .grid-list-cell-body
      .col-md-12
        h2.user-name #{ (name ? name: '-' ) + typeLabel}
        small.updated-time IP地址 #{ host}
